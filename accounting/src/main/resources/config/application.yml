server:
  port: 6009
  servlet:
    context-path: /family-accounting

spring:
  datasource:
    url: jdbc:mysql://local-mysql-serve:7799/money-keeper-dev?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: money-keeper-dev
    password: AxXSY5zNXGnDBxh7
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null
  application:
    name: family-accounting

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.family.accounting.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: ${JWT_SECRET:family-accounting-secret-key-2024}
  expiration: ${JWT_EXPIRATION:86400000}

logging:
  level: 
    com.family.accounting: DEBUG
    org.springframework.security: DEBUG
  file:
    name: /app/logs/application.log
    max-size: 10MB
    max-history: 30
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
